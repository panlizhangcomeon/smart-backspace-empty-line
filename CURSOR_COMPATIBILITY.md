# Cursor 扩展兼容性指南

## 🔍 **为什么旧扩展在 Cursor 中搜不到**

### **根本原因**：
1. **API 版本差异**：Cursor 基于较新版本的 VS Code API
2. **引擎要求**：Cursor 对扩展引擎有特定要求
3. **元数据要求**：Cursor 需要特定的元数据字段
4. **索引差异**：两个平台使用不同的搜索引擎

## 🛠️ **解决方案**

### **1. 正确的引擎配置**
```json
"engines": {
  "vscode": "^1.80.0",        // VS Code 兼容
  "cursor": ">=0.1.0"         // Cursor 兼容（重要！）
}
```

### **2. 关键词优化**
```json
"keywords": [
  "cursor",        // 包含 "cursor" 关键词
  "vscode",        // 包含 "vscode" 关键词
  "smart",         // 功能关键词
  "backspace",     // 功能关键词
  "empty",         // 功能关键词
  "line"           // 功能关键词
]
```

### **3. 完整的元数据**
```json
"author": {
  "name": "your-publisher-name"
},
"repository": {
  "type": "git",
  "url": "https://github.com/username/repo"
}
```

### **4. 分类和激活事件**
- 使用标准 `activationEvents`
- 选择正确的 `categories`
- 避免过时的 API

## 📊 **对比表**

| 特性 | VS Code 扩展 | Cursor 扩展 | 兼容性检查 |
|------|-------------|-------------|-----------|
| 引擎版本 | `vscode: ^1.80.0` | `cursor: >=0.1.0` | ✅ 必需 |
| API 调用 | 标准 VS Code API | 兼容的 API | ✅ 检查 |
| 元数据 | 基础字段 | 完整元数据 | ✅ 推荐 |
| 关键词 | 通用关键词 | 包含"cursor" | ✅ 重要 |

## 🔧 **调试步骤**

### **1. 检查引擎配置**
确保 `engines` 字段包含：
- `vscode` 引擎版本
- `cursor` 引擎版本

### **2. 验证 API 兼容性**
使用标准 VS Code API：
- ✅ `vscode.window`
- ✅ `vscode.commands`
- ✅ `vscode.extensions`
- ⚠️ 避免实验性 API

### **3. 测试元数据**
- 确保 `publisher` 字段存在
- 添加 `author` 信息
- 包含 `repository` 链接

## 📈 **最佳实践**

### **即时兼容性**
1. **引擎字段**：
   ```json
   "engines": {
     "vscode": "^1.80.0",
     "cursor": ">=0.1.0"
   }
   ```

2. **关键词策略**：
   ```json
   "keywords": [
     "cursor",     // 必含
     "vscode",     // 必含
     "your-feature"
   ]
   ```

3. **完整元数据**：
   ```json
   "publisher": "your-name",
   "author": {"name": "your-name"},
   "repository": {...}
   ```

### **发布策略**
- 首先发布到 VS Code 市场
- 验证兼容性后，再优化 Cursor
- 保持版本同步更新

## 🔗 **资源链接**

- [Cursor 扩展开发文档](https://docs.cursor.com/)
- [VS Code 扩展 API](https://code.visualstudio.com/api)
- [Publisher 管理](https://marketplace.visualstudio.com/manage/publishers)

## 💡 **总结**

通过正确的引擎配置、关键词优化和完整元数据，新扩展可以同时在 VS Code 和 Cursor 市场中被发现。关键是要明确声明 Cursor 兼容性并使用标准 API。
