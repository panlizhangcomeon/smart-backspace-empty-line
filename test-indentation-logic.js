// 极简化的智能退格功能测试
// 删除复杂的双击逻辑，直接删除空白行

class SimpleBackspaceTest {
    method1() {
        const x = 1;
        // 空白行：直接删除，检测前一行不是空白行 → 回到上一行末尾
        console.log(x);
    }
    
    method2() {
        const y = 2;
        // 空白行：直接删除，检测前一行不是空白行 → 回到上一行末尾
        if (y > 0) {
            console.log(y);
        }
    }
    
    method3() {
        // 空白行：直接删除，检测前一行是空白行 → 回到上一行缩进位置
        
        // 空白行：直接删除，检测前一行是空白行 → 回到上一行缩进位置
        
        console.log("test");
    }
    
    method4() {
        const z = 4;
            // 空白行：直接删除，检测前一行不是空白行 → 回到上一行末尾
            console.log(z);
    }
    
    method5() {
        // 多个连续空白行测试
        
        
        // 第一行空白被删除
        // 第二行空白被删除
        
        console.log("result");
    }
    
    // 极简逻辑说明：
    // 1. 当前行是空白行 → 直接删除
    // 2. 前一行是空白行 → 回到前一行缩进位置
    // 3. 前一行不是空白行 → 回到前一行末尾
    // 4. 无双击逻辑，一次退格直接删除
    // 5. 逻辑极其简单，易于理解和维护
}
