// 智能退格功能测试文件
// 在下面的空白行测试退格功能：

function example() {
    const message = "Hello World";
    console.log(message);
    
    // 第一次退格：光标应该跳到上一行末尾
    // 第二次退格：删除整个空白行
}

// 使用说明：
// 1. 将光标放在空白行上
// 2. 按 Backspace - 光标跳到上一行末尾
// 3. 再按一次 Backspace (500ms内) - 删除空白行
